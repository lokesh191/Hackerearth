t=int(input())
for _ in range(t):
    n,k,p=map(int,input().split())
    arr=list(map(int,input().split()))
    for temp in arr:
        if(temp<=p):
            p+=1
    if(p<=n):
        print(p)
    else:
        print("-1")
        
